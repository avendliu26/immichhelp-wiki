import Link from 'next/link'; import { getPublishedDocuments } from '@/lib/content';
export default function Platforms(){const docs=getPublishedDocuments().filter(d=>d.category==='Platforms');return <main className="container section"><div className="eyebrow">Platforms</div><h1>Platform guides</h1><p className="article-lead">Deployment notes for Proxmox and Synology, with platform-specific caveats kept separate from official support.</p><div className="card-grid" style={{marginTop:30}}>{docs.map(d=><Link className="card" href={`/${d.slug}`} key={d.slug}><h3>{d.title}</h3><p>{d.description}</p></Link>)}</div></main>}
