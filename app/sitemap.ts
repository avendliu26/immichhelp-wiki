import type { MetadataRoute } from 'next'; import { site } from '@/src/config/site'; import { getPublishedDocuments } from '@/lib/content'; export default function sitemap():MetadataRoute.Sitemap{const root=['','guides','setup','troubleshooting','platforms','backup','features','compare','about','privacy','terms'].map(path=>({url:`https://${site.domain}/${path}`,lastModified:new Date()}));const docs=getPublishedDocuments().map(doc=>({url:`https://${site.domain}/${doc.slug}`,lastModified:doc.updated?new Date(doc.updated):new Date()}));return [...root,...docs];}
