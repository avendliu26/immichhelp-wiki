import Link from 'next/link'; import { getPublishedDocuments } from '@/lib/content'; export default function Backup(){const docs=getPublishedDocuments().filter(d=>d.category==='Backup & Storage');return <main className="container section"><div className="eyebrow">Backup & Storage</div><h1>Protect your work</h1><p className="article-lead">Backup, restore, migrate, and verify without guessing.</p><div className="card-grid" style={{marginTop:30}}>{docs.map(d=><Link className="card" href={`/${d.slug}`} key={d.slug}><h3>{d.title}</h3><p>{d.description}</p></Link>)}</div></main>}
