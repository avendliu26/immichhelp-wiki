import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Immich Platform Guides', description: 'Platform-specific Immich notes for Synology, Proxmox and TrueNAS, with storage, firewall, container, and support boundaries clearly separated.' };
import { getPublishedDocuments } from '@/lib/content'; import { CategoryPage } from '@/components/category-page'; export default function Platforms(){const docs=getPublishedDocuments().filter(d=>d.category==='Platforms');return <CategoryPage eyebrow="Platforms" title="Platform guides" description="Deployment notes for Synology, Proxmox and TrueNAS, with platform-specific caveats kept separate from official support." docs={docs} related={[{title:'Install & Setup',href:'/setup'},{title:'Backup & Storage',href:'/backup'}]}/>}
