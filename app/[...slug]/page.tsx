import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { getDocument, getPublishedDocuments } from '@/lib/content'; import { Markdown } from '@/lib/markdown'; import { ArticleJsonLd, ArticleLayout } from '@/components/article-layout';
export function generateStaticParams(){return getPublishedDocuments().map(doc=>({slug:doc.slug.split('/')}));}
export async function generateMetadata({params}:{params:Promise<{slug:string[]}>}):Promise<Metadata>{const {slug}=await params;const doc=getDocument(slug.join('/'));if(!doc)return{};return{title:doc.title,description:doc.description,keywords:doc.keywords,alternates:{canonical:`/${doc.slug}`},openGraph:{title:doc.title,description:doc.description,type:'article'}};}
export default async function DocPage({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;const doc=getDocument(slug.join('/'));if(!doc)notFound();return <><ArticleJsonLd doc={doc}/><ArticleLayout doc={doc}><Markdown source={doc.body}/></ArticleLayout></>}
