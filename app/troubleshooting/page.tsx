import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Immich Troubleshooting', description: 'Diagnose Immich image loading and storage symptoms with source-backed checks for files, thumbnails, mounts, permissions, and versions.' };
import { getPublishedDocuments } from '@/lib/content'; import { CategoryPage } from '@/components/category-page'; export default function Troubleshooting(){const docs=getPublishedDocuments().filter(d=>d.category==='Troubleshooting');return <CategoryPage eyebrow="Troubleshooting" title="Find the fix" description="Start with symptoms, run a focused diagnosis, and choose the safest fix." docs={docs} related={[{title:'Install & Setup',href:'/setup'},{title:'Backup & Storage',href:'/backup'}]}/>}
