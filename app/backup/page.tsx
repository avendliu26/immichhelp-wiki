import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Immich Backup and Storage', description: 'Plan Immich database and media backups, external libraries, restore checks, and storage decisions with current source-backed guidance.' };
import { getPublishedDocuments } from '@/lib/content'; import { CategoryPage } from '@/components/category-page'; export default function Backup(){const docs=getPublishedDocuments().filter(d=>d.category==='Backup & Storage');return <CategoryPage eyebrow="Backup & Storage" title="Protect your work" description="Backup, restore, migrate, and verify without guessing." docs={docs} related={[{title:'Troubleshooting',href:'/troubleshooting'},{title:'Platform guides',href:'/platforms'}]}/>}
