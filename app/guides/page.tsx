import Link from 'next/link'; import { getPublishedDocuments } from '@/lib/content'; export default function Guides(){const docs=getPublishedDocuments();return <main className="container section"><div className="eyebrow">Getting Started</div><h1>Guides</h1><p className="article-lead">A practical starting point for setup, everyday workflows, and safe changes.</p><div className="card-grid" style={{marginTop:30}}>{docs.filter(d=>['Getting Started','Install & Setup'].includes(d.category)).map(d=><Link className="card" href={`/${d.slug}`} key={d.slug}><div className="card-tag">{d.category}</div><h3>{d.title}</h3><p>{d.description}</p></Link>)}</div></main>}
