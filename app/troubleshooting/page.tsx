import Link from 'next/link'; import { getPublishedDocuments } from '@/lib/content'; export default function Troubleshooting(){const docs=getPublishedDocuments().filter(d=>d.category==='Troubleshooting');return <main className="container section"><div className="eyebrow">Troubleshooting</div><h1>Find the fix</h1><p className="article-lead">Start with symptoms, run a focused diagnosis, and choose the safest fix.</p><div className="card-grid" style={{marginTop:30}}>{docs.map(d=><Link className="card" href={`/${d.slug}`} key={d.slug}><h3>{d.title}</h3><p>{d.description}</p></Link>)}</div></main>}
