import Link from 'next/link'; import { getPublishedDocuments } from '@/lib/content'; export default function Setup(){const docs=getPublishedDocuments().filter(d=>d.category==='Install & Setup');return <main className="container section"><div className="eyebrow">Install & Setup</div><h1>Setup</h1><p className="article-lead">Install, configure, verify, update, and roll back with confidence.</p><div className="card-grid" style={{marginTop:30}}>{docs.map(d=><Link className="card" href={`/${d.slug}`} key={d.slug}><h3>{d.title}</h3><p>{d.description}</p></Link>)}</div></main>}
